import { StyleSheet, Text, View } from 'react-native';

export  function Groups() {
  return (
      <Text>Groups! Hello React-Native</Text>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
